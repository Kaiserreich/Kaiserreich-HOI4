# this mtth entries can be used as variables
# example set_variable = { a = mtth:entry_name }
# and define entry_name here

WCA_state_defects_AI_weight = {
	base = 0

	modifier = {
		add = defecting_state.modifier@state_defection_chance
	}
	modifier = {
		set_temp_variable = { strength_factor = { value = USA.SPA_strength multiply = 2.5 } }
		add = strength_factor
	}
}

WCA_state_stays_loyal_AI_weight = {
	base = -100

	modifier = {
		factor = 0
		NOT = { check_variable = { defecting_state.modifier@state_defection_chance < 100 } }
	}
	modifier = {
		add = defecting_state.modifier@state_defection_chance
	}
	modifier = {
		factor = -1
	}
	modifier = {
		set_temp_variable = { strength_factor = { value = USA.SPA_strength multiply = -2.5 } }
		add = strength_factor
	}
}

APG_state_defects_AI_weight = {
	base = 0

	modifier = {
		add = defecting_state.modifier@state_defection_chance
	}
	modifier = {
		set_temp_variable = { strength_factor = { value = USA.Long_strength multiply = 2.5 } }
		add = strength_factor
	}
}

APG_state_stays_loyal_AI_weight = {
	base = -100

	modifier = {
		factor = 0
		NOT = { check_variable = { defecting_state.modifier@state_defection_chance < 100 } }
	}
	modifier = {
		add = defecting_state.modifier@state_defection_chance
	}
	modifier = {
		factor = -1
	}
	modifier = {
		set_temp_variable = { strength_factor = { value = USA.Long_strength multiply = -2.5 } }
		add = strength_factor
	}
}
