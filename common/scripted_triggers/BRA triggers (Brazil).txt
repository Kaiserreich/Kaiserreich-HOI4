BRA_has_black_monday = {
	custom_override_tooltip = {
		tooltip = {
			localization_key = tooltip_has_national_spirit
			IDEA = BRA_black_monday
		}
		OR = {
			has_idea = BRA_black_monday_1
			has_idea = BRA_black_monday_2
			has_idea = BRA_black_monday_3
			has_idea = BRA_black_monday_4
		}
	}
}
BRA_army_soft = {
	has_completed_focus = BRA_adapt_militias_into_hieracy
	NOT = { has_completed_focus = BRA_regionalist_recruiting_tactics }
}
BRA_soft_above_60_percent = {
	NOT = { check_variable = { BRA_reform_soft < 0.6 tooltip = BRA_soft_above_tooltip } }
}

BRA_soft_above_80_percent = {
	NOT = { check_variable = { BRA_reform_soft < 0.8 tooltip = BRA_soft_above_tooltip } }
}

BRA_soft_above_100_percent = {
	check_variable = { BRA_reform_soft = 1 tooltip = BRA_soft_above_tooltip }
}
BRA_army_hard = {
	has_completed_focus = BRA_federal_army
	NOT = { has_completed_focus = BRA_stronger_together }
}

BRA_hard_above_60_percent = {
	NOT = { check_variable = { BRA_reform_hard < 0.6 tooltip = BRA_hard_above_tooltip } }
}

BRA_hard_above_80_percent = {
	NOT = { check_variable = { BRA_reform_hard < 0.8 tooltip = BRA_hard_above_tooltip } }
}

BRA_hard_above_100_percent = {
	check_variable = { BRA_reform_hard = 1 tooltip = BRA_hard_above_tooltip }
}

PAR_BRA_is_busy = {
	BRA = {
		OR = {
			exists = no
			has_war_with = INT
			has_war_with = ENT
			has_war_with = GER
			has_war_with = BAH #civil war Brazil
			has_war_with = SPO #civil war Brazil
			is_subject = yes
		}
	}
}
